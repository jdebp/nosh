## **************************************************************************
## For copyright and licensing terms, see the file named COPYING.
## **************************************************************************

[Unit]
Description=sysctl settings

[Service]
ExecStart=sh -c 'for f in /etc/sysctl.conf /etc/sysctl.conf.local /etc/service-bundles/convert/sysctl.conf ; do [ -r $f ] && sysctl -i -f $f ; done'
RemainAfterExit=yes

[Install]
WantedBy=workstation.target
