## **************************************************************************
## For copyright and licensing terms, see the file named COPYING.
## **************************************************************************

[Unit]
Description=kmod loader for ipv6

[Service]
systemdWorkingDirectory=false
ExecStart=sh -c '[ -d /sys/module/ipv6/ ] || kmod modprobe ipv6'
RemainAfterExit=yes

[Install]
WantedBy=workstation.target
