#
# Enable SSH Server services
#
# This is not a systemd preset.
# It goes in a special nosh system-manager preset directory.
# It is used by the nosh-run-openssh-server package in order to enable the services themselves.
#

enable	cyclog@sshd.service
enable	sshd.service
enable	cyclog@ssh.service
enable	ssh.service
enable	cyclog@sshgenkeys.service
enable	sshgenkeys.service
