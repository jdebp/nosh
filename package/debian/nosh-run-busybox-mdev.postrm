#!/bin/sh -
case "$1" in
	upgrade)
		for i in \
			busybox-mdev \
			busybox-mdev-log \
			busybox-mdev-rescan \
			;
		do
			if system-control is-loaded $i.service
			then
				system-control try-restart $i.service
			fi
		done
		;;
esac

